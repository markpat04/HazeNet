# HazeNet pipeline — pinned for reproducible local + RunPod runs.
# On RunPod, start from a CUDA PyTorch base image and `pip install -r requirements.txt`
# (torch is provided by the base image there; install it locally per pytorch.org).
torch>=2.2
numpy>=1.26
pandas>=2.0
xarray>=2024.1
zarr>=2.17
netCDF4>=1.6
scipy>=1.11
rioxarray>=0.15
rasterio>=1.3
matplotlib>=3.8
pyyaml>=6.0
requests>=2.31
cdsapi>=0.7        # CDS ERA5 (blh, t850, winds, precip, rh, temp)
